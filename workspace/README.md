# Workspace

This directory is reserved for temporary local artifacts used during manual testing
or ad hoc analysis runs (for example, copied input audio clips or intermediate
JSON outputs).

Notes:

- Files here are not required for normal agent execution.
- Keep sensitive audio out of version control.
- Prefer cleaning this directory between test runs.
